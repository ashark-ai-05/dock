[package]
name = "dock-testing"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Timing budgets for Dock's tests. Not published; a dev-dependency of every Dock crate."
publish = false
